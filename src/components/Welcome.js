import { Typography } from '@material-ui/core'
export default function Welcome(){
    return(
        <center>
            <Typography variant="h3" color="primary">
                Bienvenida Violeta ! Tienes 
            </Typography>
            <Typography variant="h3" color="secondary">
                400
            </Typography>
            <Typography variant="h3" color="primary">
                Puntos  
            </Typography>
        </center>
    );
}